@model List<string>





@if (Model.Count == 0)
{
    <div>You don't have any order data to display</div>
}
else
{
    foreach (var id in Model)
    {
        <div>@Html.ActionLink(id, "ShowOrderDetails", "Account", new { transId = id }, null)</div>
    }
}

